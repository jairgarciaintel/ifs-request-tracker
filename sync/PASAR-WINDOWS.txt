============================================================
   MIGRAR EL KIRO "dev-dashboard" A jairgarciaintel (WINDOWS)
============================================================
YA MIGRADO (2026-08-26). Este archivo queda como referencia.
El token viejo fue REVOCADO por seguridad (habia quedado expuesto en un log).

Si necesitas re-configurar el remote del repo dev en alguna compu:
  git remote set-url origin "https://jairgarciaintel:<PAT_NUEVO>@github.com/jairgarciaintel/dashboards-strategy-2026.git"
  git pull origin main

El <PAT_NUEVO> se pasa entre compus por MEGA/archivo local, NUNCA por el repo
publico ni pegando logs con el token dentro.

REGLA: nunca pegar la salida de "git remote -v" en un archivo del repo,
porque incluye el token completo.
============================================================
