============================================================
   NOTA: el git ya quedo arreglado (credential.helper store).
   Instructivos: dev/ARREGLAR-GIT-DEFINITIVO.txt (repo DEV)
============================================================
NO poner el token ni URLs con sig= aqui (repo publico). Los logs largos del flow
traen la firma sig= en los headers -> NO pegarlos completos aqui.

============================================================
   PENDIENTE 2 - ASSIGNED TO  ->>> RESUELTO (2026-08-27)
============================================================
El campo "iGO Admin Only - Assigned To Claims" del flow Update Fields se cambio a:
    triggerBody()?['assignedToClaim']
(antes tenia outputs('Get_item')... que copiaba el valor viejo -> por eso no cambiaba).

Confirmado en el Run history:
- Input del PatchItem:  item/iGOAdminOnly_x002d_AssignedTo/Claims = el claim correcto
  (probado: jenn.glavan y jair.garcia).
- statusCode 200.
- Output de SharePoint: iGOAdminOnly_x002d_AssignedTo quedo con la persona asignada
  (Jenn cuando se mando Jenn, Jair cuando se mando Jair).
=> SharePoint YA guarda el Assigned To correctamente.

CERRADO DEFINITIVO (2026-08-27) - FUNCIONA:
- Se asigna desde el tracker -> se guarda en SharePoint -> se ve en SharePoint
  directo Y en el tracker. Confirmado con el request 2712.
- El fix final: en el flow Update Fields, el campo "iGO Admin Only - Assigned To
  Claims" usa la expresion triggerBody()?['assignedToClaim'] (no outputs Get_item).
- Se agrego un "Get item" al final del flow para verificar; ayudo a confirmar que
  quedaba guardado.
=> PENDIENTE 2 CERRADO. Pendiente 4 (lectura) tambien OK para este campo.
